# PromptCraft 使用指南

> 适用版本：PromptCraft 0.1.0 Windows 便携版  
> 适用人群：第一次使用 API、不了解提示词结构的零基础用户

这是作者第一次公开开源的软件项目。如果你在体验过程中发现问题、困惑或可以优化的地方，欢迎提交 GitHub Issue，也可以发送邮件至 `3986351310@qq.com`。反馈时请先删除 API Key、密码和其他敏感信息。

PromptCraft 是一个本地提示词增强工具。你可以把一句不够完整的需求交给它，它会调用你自己的 DeepSeek API，将需求整理成更清楚、更容易让大模型理解的提示词。

请先理解下面两个边界：

1. PromptCraft 负责优化提示词，不负责生成问题的最终答案。
2. “复制并打开”只会复制增强后的提示词并打开目标网页，不会自动粘贴或发送。打开网页后，需要你自己按 `Ctrl + V` 粘贴并发送。

---

## 第一部分：设置和配置 API

### 1. API 是什么

API 可以理解为软件与大模型之间的连接通道。PromptCraft 需要通过 DeepSeek API 才能增强提示词。

本软件不赠送 API 额度，也不会替你充值。你需要使用自己的 DeepSeek 账户创建 API Key，并由自己的账户承担调用费用。

### 2. 准备 DeepSeek API Key

1. 使用浏览器进入 DeepSeek 开放平台。
2. 登录或注册自己的账户。
3. 进入 API Key 管理页面。
4. 创建一个新的 API Key。
5. 创建后立即保存好完整 Key。它通常以 `sk-` 开头，并且平台可能只完整显示一次。

注意事项：

- API Key 相当于账户密码，不要发给其他人。
- 不要把 API Key 粘贴到普通提示词、聊天记录或附件中。
- 如果怀疑 Key 已经泄露，应立即前往供应商平台删除旧 Key，并创建新 Key。

### 3. 打开供应商设置

1. 解压便携版 ZIP。
2. 双击 `PromptCraft.exe`。
3. 在左侧导航栏点击“设置”。
4. 找到“DeepSeek 供应商”区域。

首次运行时，Windows 可能显示 SmartScreen 提示。这是因为当前版本没有代码签名。请确认程序来自可信位置后，再选择“更多信息”并继续运行。

### 4. 填写 API 配置

按下面的说明填写设置页：

| 设置项 | 建议填写内容 | 说明 |
| --- | --- | --- |
| API Base URL | `https://api.deepseek.com` | 使用官方 DeepSeek API 时保留默认值。只有明确使用其他兼容服务时才修改。 |
| API Key | 你的完整 API Key | 只粘贴到这个密码输入框中。保存后软件不会再次显示完整内容。 |
| V4-Flash 模型 ID | `deepseek-v4-flash` | 已通过实际 API 响应确认。不要填写展示名称 `V4-Flash`。 |
| 默认模型 | DeepSeek Chat 或 V4-Flash | 选择以后启动软件时默认使用的增强模型。 |

### 5. 选择模型

- **DeepSeek Chat**：默认模型，适合一般学习、办公和代码任务。
- **V4-Flash**：实际 API 模型 ID 为 `deepseek-v4-flash`。只有你的 API 服务和账户具有该模型权限时才能使用。

模型不能使用时，软件不会偷偷切换到另一个模型。请根据错误提示检查模型 ID、账户权限或供应商支持情况。

### 6. 填写价格和费用额度

在“费用控制”区域填写：

- **输入价格**：供应商当前公布的每千 token 输入价格。
- **输出价格**：供应商当前公布的每千 token 输出价格。
- **月度提醒额度**：达到后显示提醒，但仍允许继续调用。
- **月度强制额度**：预计本次请求会超过该金额时，软件会在调用前阻止请求。

价格可能随供应商调整，请以供应商最新计费页面为准。软件显示的费用只是本机估算，最终账单以供应商为准。

将提醒额度或强制额度设为 `0`，表示关闭该限制。

### 7. 验证并保存

1. 检查所有字段。
2. 点击“验证并保存”。
3. 等待软件连接 API。
4. 出现“连接验证成功，配置已保存”后，即可正常使用。

API Key 会保存到 Windows 凭据管理器，不会以明文写入 PromptCraft 数据库、日志或导出包。

以后如果只修改价格、默认模型或模型 ID，可以让 API Key 输入框保持空白。空白表示继续使用已经保存的 Key，不是删除 Key。

---

## 第二部分：正常使用流程

### 1. 输入原始需求

点击左侧“增强”，在“原始需求”文本框中输入想让 AI 帮你完成的事情。

例如：

```text
中暑症状表现
```

提示词可以很短。软件会分析缺少的目标、受众、范围、输出要求和风险保护，但不会为了显得专业而机械扩写。

原始需求不能为空，只输入空格也无法开始增强。

### 2. 添加上下文

如果任务依赖前面的聊天、项目背景、课程要求或写作规范，可以粘贴到“上下文”区域。

适合放入上下文的内容包括：

- 前面的聊天记录。
- 作业或比赛要求。
- 项目背景和代码说明。
- 文稿受众、字数和语气要求。
- 必须保留或禁止出现的内容。

聊天记录只会被视为参考资料。PromptCraft 不会自动把聊天记录中的最后一句当成新命令。

### 3. 添加附件

点击“添加文件”可以导入本地资料。当前版本支持：

- 纯文本：TXT、Markdown、JSON、CSV。
- 常见代码文件：Python、JavaScript、TypeScript、Java、C、C++、Rust、Go、HTML、CSS、SQL 等。
- 带有可复制文字层的 PDF。
- DOCX 文档。

附件限制：

- 一次最多 5 个文件。
- 单个文件不能超过 10 MB。
- 上下文和附件提取文字合计不能超过 100,000 个字符。
- 不支持截图、图片 OCR、扫描版 PDF、Excel、PPT、压缩包和整个项目文件夹。

软件只读取原文件并提取文字，不会修改或删除你的原文件。附件中的内容始终是参考资料，不能覆盖 PromptCraft 的安全规则。

### 4. 选择本次增强设置

在顶部工具栏选择：

1. **增强模型**：选择 DeepSeek Chat 或 V4-Flash。
2. **目标网页**：选择豆包、DeepSeek 网页版、千问、MiniMax 或自定义网页。
3. **详细程度**：选择简洁、标准、深入或自定义。

详细程度说明：

| 档位 | 适用情况 |
| --- | --- |
| 简洁 | 原需求基本清楚，只需要消除歧义和补充关键条件。 |
| 标准 | 大多数学习、办公和日常任务，建议新手优先使用。 |
| 深入 | 复杂代码、项目分析、长文稿和需要验收标准的任务。 |
| 自定义 | 需要自己规定长度、必须包含内容或禁止添加内容。 |

选择“自定义网页”后，还需要填写完整网址，例如 `https://example.com/`。

### 5. 开始增强

1. 点击“增强”。
2. 软件先进行本地字符数、敏感内容和费用检查。
3. 如果检测到隐私、凭据、商业信息或高风险操作，会先显示确认窗口。
4. 确认后，软件开始调用 DeepSeek，并在右侧逐步显示结果。

API Key、密码和私钥即使经过用户确认，也会在发送前被自动遮蔽。身份证号、商业机密等其他敏感内容需要你确认后才能继续。

### 6. 停止或重新生成

- 生成过程中点击“停止”，软件会取消当前请求，并保留已经收到的文字。
- 停止后的内容可以复制或手动编辑，但会标记为未完成。
- 需要重新尝试时，再次点击增强或重新生成。
- 网络错误、流读取中断或返回结构错误时，软件会自动重试一次。

V4-Flash 的完整生成可能超过 30 秒。软件使用 30 秒“无数据空闲超时”，只要模型仍在持续返回内容，就不会因为总耗时超过 30 秒而中断。

### 7. 回答澄清问题

当原始需求缺少真正影响结果的信息时，软件可能提出最多 3 个问题。

此时你可以：

- 逐项填写答案，然后点击“提交并继续增强”。
- 不知道答案时留空，跳过该问题。
- 点击“结束澄清”，直接使用当前临时版本。

最多进行 3 轮澄清。即使信息不足，软件也会保留一份基于明确假设的临时提示词，不会只显示问题而不给结果。

### 8. 检查和编辑结果

右侧“增强结果”文本框中的内容可以直接编辑。

下方“修改明细”会说明：

- 原文是什么。
- 模型改成了什么。
- 为什么修改。
- 修改属于澄清、补充上下文、添加约束、格式调整还是安全保护。

每项修改右侧有接受和拒绝按钮。拒绝后，软件会尽量恢复对应的原文片段。

顶部的撤销和重做按钮可以恢复最近的编辑、候选应用和修改决定。单个会话最多保留最近 100 个撤销快照。

### 9. 使用 5 个可选补充

完整生成后，页面底部会显示 5 个候选建议。它们可能用于补充目标、受众、格式、约束或另一种合理意图。

1. 点击某个候选建议。
2. 阅读插入内容预览。
3. 确认适合当前任务后，点击“加入提示词”。
4. 不适合时点击“取消”。

候选不会默认加入。同一个候选只能应用一次，并且可以通过撤销恢复。

### 10. 复制并发送给目标模型

确认提示词没有问题后，可以：

- 点击“复制”，只复制当前提示词。
- 点击“复制并打开”，复制提示词并打开所选目标网页。

网页打开后：

1. 点击目标模型的输入框。
2. 按 `Ctrl + V` 粘贴。
3. 再次检查内容。
4. 自己点击发送。

PromptCraft 不会自动控制网页、自动粘贴或自动发送。

如果启用了剪贴板清理，复制内容会在 2 分钟后自动清除。只有剪贴板仍然是 PromptCraft 写入的同一段文字时才会清理，不会覆盖你后来复制的新内容。

### 11. 使用历史、画像和数据功能

#### 历史记录

- 每次完成增强后，会自动保存原始需求和增强结果。
- 点击左侧“历史”可以搜索、恢复或删除记录。
- 恢复历史不会删除原记录。

#### 本地偏好画像

- 点击左侧“画像”可以查看、修改或删除偏好。
- 关闭“已启用”开关后，当前请求不会再向 DeepSeek 发送画像摘要。
- 当前请求中的明确要求始终高于历史偏好。

#### 导出和导入

- 设置页可以将历史、设置和不含密钥的供应商配置导出为 ZIP。
- 导出包不包含 API Key、附件原件和操作日志。
- 导入时会检查版本、大小、文件类型和 ZIP 路径安全。
- 导入记录会生成新 ID，不会覆盖已有历史记录。

#### 清空数据

“清空全部本地数据”会删除历史、画像、费用记录、设置和 Windows 凭据管理器中的 API Key。该操作无法撤销。

受 SSD 磨损均衡机制影响，软件可以删除数据库和缓存，但无法保证底层存储数据绝对不可恢复。

---

## 第三部分：常见异常和解决方案

### 1. 点击增强后提示“尚未配置 DeepSeek API Key”

可能原因：API Key 没有成功保存，或者已经在 Windows 凭据管理器中被删除。

解决方法：

1. 打开“设置”。
2. 重新填写完整 API Key。
3. 点击“验证并保存”。
4. 验证成功后返回增强页面重试。

### 2. 提示“API Key 无效或没有模型权限”

可能原因：

- Key 复制不完整。
- Key 已被删除或禁用。
- 当前账户没有所选模型权限。
- API Base URL 与 Key 不属于同一个服务。

解决方法：重新创建 Key，确认 Base URL，并在供应商平台检查模型权限。

### 3. 提示模型名称错误

如果错误中出现类似下面的内容：

```text
The supported API model names are deepseek-v4-pro or deepseek-v4-flash
```

请打开设置，将 V4-Flash 模型 ID 改为：

```text
deepseek-v4-flash
```

不要填写展示名称 `V4-Flash`。修改后保存并重新生成。

### 4. 提示账户余额不足

API 供应商账户余额不足时，PromptCraft 无法继续调用。

请前往供应商平台查看余额、套餐和账单，完成充值后再试。PromptCraft 不代收费用。

### 5. 提示请求过于频繁或 HTTP 429

这是供应商的限流提示。

请等待一段时间后重试，避免连续快速点击增强。生成期间增强按钮会切换为停止按钮，不能同时启动第二个请求。

### 6. 一直生成，或者提示流式响应中断

软件会在网络请求或流读取失败时自动重试一次。

仍然失败时：

1. 检查网络是否稳定。
2. 检查防火墙或代理是否阻止 API 地址。
3. 切换到 DeepSeek Chat 再试。
4. 缩短上下文或减少附件。
5. 稍后再试，排除供应商服务波动。

已收到的文字会保留。连续 30 秒没有收到任何数据时，软件才会判定流读取超时。

### 7. 软件提示模型返回结构无效

模型偶尔可能没有按要求返回完整 JSON、5 个建议或正确字段。

软件会自动重新请求一次。如果第二次仍失败，请点击增强重新生成，或者切换模型后重试。你的原始输入不会丢失。

### 8. 点击增强没有反应

请依次检查：

1. 原始需求是否为空。
2. 是否只输入了空格。
3. 是否打开了敏感信息确认窗口但没有处理。
4. 是否已经达到月度强制额度。
5. V4-Flash 模型 ID 是否为空。
6. 页面顶部是否显示具体错误。

### 9. 提示超过月度强制额度

软件会估算本次输入和预期输出费用。预计调用后超过强制额度时，请求不会发送。

你可以打开设置查看额度，但调整前应先核对供应商账单。关闭额度限制时将强制额度设为 `0`。

### 10. 附件无法添加

检查以下限制：

- 是否已经添加 5 个附件。
- 单个文件是否超过 10 MB。
- 文件格式是否受支持。
- 文件是否被移动、删除或被其他程序独占。
- 文件内容是否是可识别的文本。

### 11. PDF 提示不支持扫描版

扫描版 PDF 的每一页实际是图片，没有可提取文字层。当前版本不提供 OCR。

可以先使用可信的 OCR 工具将内容转换为 TXT、Markdown 或 DOCX，检查识别结果后再导入。

### 12. 文件编码无法识别

建议使用文本编辑器将文件另存为 UTF-8 编码，然后重新添加。不要为了导入成功而随意修改二进制文件扩展名。

### 13. 提示上下文超过 100,000 字符

删除无关聊天记录，只保留与本次任务直接相关的段落。大型文档可以拆分后分次处理。

### 14. 出现敏感信息确认窗口

先查看软件检测到了什么：

- API Key、密码、私钥会被强制遮蔽。
- 身份证号、商业机密、未公开资料需要你判断是否应该发送。
- 删除、清空和批量修改类任务会提示影响范围和保护措施。

不确定时请选择返回检查，删除敏感内容后再增强。

### 15. 点击“复制并打开”后没有自动粘贴

这是正常设计。软件只复制内容并打开网页，不会控制网页。

请点击网页输入框，再按 `Ctrl + V` 粘贴。

### 16. 目标网页没有打开

请检查：

- Windows 是否设置了默认浏览器。
- 自定义网址是否包含 `http://` 或 `https://`。
- 浏览器或安全软件是否阻止新窗口。

提示词通常已经复制，可以手动打开网页后粘贴。

### 17. 复制失败或两分钟后内容消失

其他程序可能正在占用剪贴板。请再次点击复制。

如果启用了“2 分钟后清理”，内容按设计自动清除。可以在设置中关闭该功能。

### 18. Windows 阻止软件运行

当前便携版没有代码签名，可能出现 SmartScreen 提示。

请确认 ZIP 来源和 SHA-256 校验值无误，然后选择“更多信息”继续运行。不要绕过来源不明程序的安全警告。

### 19. 软件启动后空白或提示缺少 WebView2

便携目录中包含微软官方 `MicrosoftEdgeWebView2Setup.exe`。软件检测到缺少 WebView2 时会尝试静默安装，该过程需要联网。

仍然失败时，请确认：

- 便携包已经完整解压。
- `PromptCraft.exe` 和 WebView2 安装程序仍在同一目录。
- 当前网络可以连接微软下载服务。
- 安全软件没有拦截安装程序。

### 20. 历史记录或导入包无法读取

导入包必须是由兼容版本 PromptCraft 导出的 ZIP。不要手动修改包内文件名、目录结构或 JSON 字段。

如果 ZIP 已损坏，请重新导出。导入不会读取 API Key，也不会覆盖已有历史 ID。

### 21. 清空后数据还能恢复吗

清空操作会删除应用保存的历史、设置、费用记录和 API 凭据，并清理应用数据库内容。

但任何软件都无法保证 SSD 底层物理数据绝对不可恢复。高度敏感设备应结合磁盘加密、系统账户保护和专业的数据销毁制度。

### 22. 仍然无法解决

记录以下信息，便于定位问题：

- 出错时间。
- 使用的增强模型。
- 错误提示原文。
- 是否使用附件，以及附件格式和大小。
- 问题发生在验证 API、开始增强、流式生成、复制打开还是导入导出阶段。

不要在截图或问题描述中包含完整 API Key、密码、身份证号或未公开资料。
